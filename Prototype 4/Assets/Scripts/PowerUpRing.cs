using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerUpRing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //transform.Rotate(Vector3.up, 0.5f);
        //transform.position = new Vector3(GameObject.Find("Player").transform.position.x,
        //    GameObject.Find("Player").transform.position.y - 0.2f,
        //    GameObject.Find("Player").transform.position.z);
    }
}
